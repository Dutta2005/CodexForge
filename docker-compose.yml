services:
  web:
    build: .
    ports: ["3000:3000"]
    environment:
      NEXT_PUBLIC_API_URL: http://backend:8000
      NEXT_PUBLIC_SOCKET_URL: http://localhost:8000
  backend:
    build: ./backend
    ports: ["8000:8000"]
    volumes: ["./data:/data"]
