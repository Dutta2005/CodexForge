'use client';import { WorkspaceShell } from '@/components/workspace/shell';import { Card } from '@/components/ui/card';import { graphEdges, graphNodes } from '@/lib/mock-data';
export default function Architecture(){return <WorkspaceShell><h1 className="text-3xl font-semibold">Architecture View</h1><div className="mt-6 grid gap-4 lg:grid-cols-[2fr_1fr]"><Card><div className="relative h-[520px] overflow-hidden rounded-xl bg-black/30">{graphNodes.map((n,i)=><div key={n.id} className="absolute rounded-2xl border border-cyan-300/30 bg-white/10 px-4 py-3" style={{left:80+(i%2)*300,top:60+i*80}}>{n.label}<p className="text-xs text-slate-400">{n.type}</p></div>)}<svg className="absolute inset-0 h-full w-full">{graphEdges.map((e,i)=><line key={e.id} x1={170+(i%2)*300} y1={100+i*80} x2={470-((i+1)%2)*300} y2={180+i*80} stroke="rgba(34,211,238,.5)"/>)}</svg></div></Card><Card><h2 className="font-semibold">Detected layers</h2>{graphNodes.map(n=><div className="mt-3 rounded-xl bg-black/20 p-3" key={n.id}>{n.label}<p className="text-xs text-slate-400">Relationships, routes, and file ownership indexed.</p></div>)}</Card></div></WorkspaceShell>}
