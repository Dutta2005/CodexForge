import { WorkspaceShell } from '@/components/workspace/shell';import { Card } from '@/components/ui/card';import { repositories, stats, taskRuns } from '@/lib/mock-data';
export default function Dashboard(){return <WorkspaceShell><h1 className="text-3xl font-semibold">Dashboard</h1><div className="mt-6 grid gap-4 md:grid-cols-4">{Object.entries(stats).map(([k,v])=><Card key={k}><p className="text-sm capitalize text-slate-400">{k}</p><p className="mt-2 text-3xl font-bold">{v}</p></Card>)}</div><div className="mt-6 grid gap-4 lg:grid-cols-2"><Card><h2 className="font-semibold">Imported repositories</h2>{repositories.map(r=><div className="mt-4 rounded-xl bg-black/20 p-4" key={r.id}><b>{r.name}</b><p className="text-sm text-slate-400">{r.framework} · {r.languages.join(', ')}</p></div>)}</Card><Card><h2 className="font-semibold">Recent tasks</h2>{taskRuns.map(t=><div className="mt-4 rounded-xl bg-black/20 p-4" key={t.id}><b>{t.title}</b><p className="text-sm text-emerald-300">{t.status} · {t.tests.passed} tests passed · {t.tests.coverage}% coverage</p></div>)}</Card></div></WorkspaceShell>}
