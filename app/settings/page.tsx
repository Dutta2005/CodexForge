import { WorkspaceShell } from '@/components/workspace/shell';import { Card } from '@/components/ui/card';
export default function Settings(){return <WorkspaceShell><h1 className="text-3xl font-semibold">Settings</h1><div className="mt-6 grid gap-4 md:grid-cols-2">{['GitHub OAuth','AI provider','Theme','Preferences'].map(s=><Card key={s}><h2 className="font-semibold">{s}</h2><p className="mt-2 text-sm text-slate-400">Secure configuration with environment-backed defaults and session persistence.</p></Card>)}</div></WorkspaceShell>}
